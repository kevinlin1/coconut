{
  "name": "coconut-checks",
  "version": "0.0.0",
  "private": true,
  "description": "Accessibility checks for the coconut Typst package. Not part of the published package.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "build": ".github/scripts/build.sh",
    "axe": "node .github/scripts/axe-check.mjs",
    "alt": "node .github/scripts/alt-text-check.mjs",
    "test": "node --test .github/scripts",
    "check": "npm run build && npm run axe -- build/site build/reader && npm run alt -- build/site build/reader && npm test",
    "pages": "npm run build && .github/scripts/pages.sh build build/pages"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.10.2",
    "playwright": "^1.56.0"
  }
}
